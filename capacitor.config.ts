import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'One_Piece_Firestore',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
